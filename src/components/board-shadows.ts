import { StyleSheet } from "react-native";

export const shadow = StyleSheet.create({
  card: {
    boxShadow: "0px 2px 16px rgba(0, 0, 0, 0.04)"
  },
  fab: {
     boxShadow: "0px 6px 32px rgba(0, 122, 255, 0.3)",
  },
});
